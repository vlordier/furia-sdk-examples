# hello-ew

Demonstrates the `EWSimProvider` SDK trait — electronic warfare simulation with jamming and ESM sweeps.

## What it shows

- Implementing `EWSimProvider` for jammer configuration and tick
- Performing ESM detection sweeps and spectrum management
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-ew
```