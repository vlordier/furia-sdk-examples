# Screenshots

Screenshots of the example crates in action will be added here once the
SDK examples are built against a running furia-control instance.

## Planned screenshots

| Screenshot | Shows |
|-----------|-------|
| hello-policy-demo.png | PolicyProvider evaluating IHL rules |
| hello-simulation-demo.png | SimulationProvider entity lifecycle |

Each example can be run standalone:
```bash
cargo run -p hello-policy
cargo run -p hello-simulation
```