# hello-acoustic

Demonstrates the `AcousticProvider` SDK trait — passive sonar detection and underwater acoustic simulation.

## What it shows

- Implementing `AcousticProvider` for sonar detection
- Configuring sonar arrays and ticking acoustic propagation
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-acoustic
```