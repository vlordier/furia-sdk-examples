# hello-assessment

Demonstrates the `AssessmentEngine` SDK trait — battle damage assessment (BDA).

## What it shows

- Implementing `AssessmentEngine` for impact detection
- Evaluating target status and collateral from simulation events
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-assessment
```