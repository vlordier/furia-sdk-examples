# hello-ui

Demonstrates the `UiPlugin` SDK trait — SolidJS UI plugin integration with FuriaBuilder.

## What it shows

- Implementing `UiPlugin` for custom panel rendering
- Declaring metadata (name, dimensions, preferred slot)
- Generating render output from plugin props
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-ui
```