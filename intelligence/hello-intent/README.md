# hello-intent

Demonstrates the `IntentProvider` SDK trait — parsing commander intent text into structured directives.

## What it shows

- Implementing `IntentProvider` for keyword-based intent parsing
- Extracting objective, constraints, and target from natural language
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-intent
```