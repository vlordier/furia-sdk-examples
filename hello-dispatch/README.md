# hello-dispatch

Demonstrates the `DispatchAdapter` SDK trait — delivering actions to external platforms.

## What it shows

- Implementing `DispatchAdapter` for target validation and action dispatch
- Returning structured `DispatchReceipt` with ack status
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-dispatch
```