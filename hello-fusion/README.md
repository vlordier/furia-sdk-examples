# hello-fusion

Demonstrates the `FusionEngine` SDK trait — correlating sensor tracks from multiple sources.

## What it shows

- Implementing `FusionEngine` for track correlation
- Determining SameEntity vs Unrelated tracks by prefix matching
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-fusion
```