# hello-policy

Demonstrates the `PolicyProvider` SDK trait — IHL and ROE evaluation for engagement decisions.

## What it shows

- Implementing `PolicyProvider` for civilian/threat distinction
- Returning Allow, Deny, or Escalate decisions
- Generating intent feedback with policy constraints
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-policy
```