# hello-decision-tree

Demonstrates the `DecisionTreeProvider` SDK trait — training and evaluating threshold-based classifiers.

## What it shows

- Implementing `DecisionTreeProvider` for feature classification
- Training a tree from labelled examples and evaluating new inputs
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-decision-tree
```