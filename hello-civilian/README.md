# hello-civilian

Demonstrates the `CivilianDensityProvider` SDK trait — population modelling and IHL proportionality assessment.

## What it shows

- Implementing `CivilianDensityProvider` for regional population density
- Estimating collateral casualties from weapon effects
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-civilian
```