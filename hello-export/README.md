# hello-export

Demonstrates the `ExportAdapter` SDK trait — converting internal entities to standardised interoperability formats.

## What it shows

- Implementing `ExportAdapter` for JSON serialisation
- Reporting supported export formats
- Health/version via `ModuleHandle`

## Run

```bash
cargo run -p hello-export
```